# TierSum

<div class="text-center mb-8">
  <p class="text-xl text-slate-400 mb-4">您的知识，<strong class="text-slate-100">分层有序</strong></p>
  <p class="text-slate-500 mb-6">TierSum 是一个无需 RAG 的文档检索系统，通过多层抽象保留文档结构。热文档使用 LLM 语义分析章节抽取 → 渐进式查询；冷文档使用 Markdown 语法章节抽取 → BM25 倒排 + 向量混合检索。全部以章节粒度处理，非固定尺寸切块。</p>
  <div class="flex justify-center gap-4">
    <a href="/site/documentation" class="btn btn-primary">开始使用</a>
    <a href="/site/about" class="btn btn-outline border-slate-600">了解更多</a>
  </div>
</div>

---

## 为何选择 TierSum

常见检索会把长文切成大量小块再做相似度匹配，容易**丢失结构与语境**。TierSum 用**分层摘要**（文档 → 章节 → 原文）保留知识结构，并配合**标签与主题**做可解释的导航与筛选，而不是只靠向量「猜」片段边界。

**热路径**上，入库时即可由 LLM 进行语义分析章节抽取，生成标签、文档概述与章节级提要，形成**预摘要语义层**，为**渐进式查询**所复用——沿**标签 → 文档 → 章节**逐步收窄，每一步由 LLM 评分，如同先扫目录再翻到对应小节。**冷路径**使用 **Markdown 语法章节抽取**：按标题将内容拆分为自然章节（非固定尺寸碎片），使用 BM25 倒排索引 + HNSW 向量混合检索，命中返回**整章正文**。冷热两档均以章节为粒度保证语义完整性。访问频繁时可手动或自动升级为热文档。

> **冷热一致：以章节为单元。** 无论走「热」还是「冷」，系统都以 **Markdown 章节**（按标题划分的自然段）作为工作与检索粒度，而不是固定长度的随机碎片。摘要、渐进式收窄与冷索引检索都沿这一边界展开，**保证语义从头到尾完整、可理解**。

---

## 功能特性

### 章节优先的文档处理

传统 RAG 系统将文档切成任意碎片，破坏结构与语境。TierSum 按标题解析 Markdown，形成自然的章节层级，与人类书写和阅读方式一致。

- 标题感知的拆分保留文档结构
- 每章节可配置 token 预算
- 长节支持滑动窗口
- 每个章节拥有稳定的路径标识

### 热 / 冷分层

为每篇文档选择合适的入库路径，在查询质量与成本之间取得平衡。两种路径均以**章节为粒度**——非固定尺寸切块——端到端保证语义完整性。

- **热**：LLM 语义分析章节抽取——完整 LLM 分析生成摘要、标签与章节分析，为渐进式查询的多阶段排序（标签 → 文档 → 章节）提供预摘要层
- **冷**：Markdown 语法章节抽取——按标题将内容拆分为自然章节，使用 BM25 倒排索引 + HNSW 向量混合检索，入库时不调用 LLM
- **自动模式**根据内容长度智能选择
- 冷文档可通过手动或自动方式升级为热文档

### 渐进式查询

不同于单一的向量相似度搜索，TierSum 使用多阶段管道模拟人类搜索方式：先按主题收窄，再到文档，最后到章节。

1. **标签筛选** — 从查询中找到相关的目录标签
2. **文档排序** — 用 LLM 相关性对匹配文档打分
3. **章节选择** — 从排序后的文档中挑选最佳章节
4. **可选**：汇总为带引用的回答

### 主题与标签

自动生成的目录标签，按 LLM 策展的主题分组。像人类组织知识一样浏览您的知识库。

### 双轨认证

人类与程序使用独立的认证路径。人类使用浏览器会话（支持通行密钥），程序使用带作用域的 API 密钥。

- 基于角色的访问控制：viewer、user、admin
- API 密钥作用域：read、write、admin
- 持久会话的设备令牌
- 速率限制与暴力破解防护

### REST + MCP

完整的 REST API 用于集成。支持 MCP（模型上下文协议），用于 AI 代理和自动化工作流。

---

## 工作原理

### 01 — 摄入

上传 Markdown 文档。选择热（完整 LLM 分析）或冷（仅索引）摄入模式。自动模式根据内容长度和配额选择最佳路径。

### 02 — 索引

热文档获得章节级摘要和标签。冷文档拆分为章节，并使用 BM25 + HNSW 向量搜索索引。全部保留标题结构。

### 03 — 查询

用自然语言提问。渐进式查询遍历标签 → 文档 → 章节。获取带引用的综合答案，或浏览原始章节命中结果。

---

## 技术栈

| 技术 | 用途 |
|-----------|---------|
| Go 1.23+ | 后端 |
| Vue 3 | 前端 |
| SQLite / PostgreSQL | 数据库 |
| Bleve + HNSW | 搜索 |
| OpenAI / Claude / Ollama | LLM |
| Prometheus | 指标 |
| MCP | 协议 |
| Docker | 部署 |

---

## 快速开始

### 1. 前置条件

- Go 1.23 或更高版本
- Make
- OpenAI API 密钥（或 Anthropic，或本地 Ollama）

### 2. 安装

```bash
# 克隆仓库
git clone https://github.com/tiersum/tiersum.git
cd tiersum

# 复制并编辑配置
cp configs/config.example.yaml configs/config.yaml
# 编辑 configs/config.yaml 并设置您的 LLM API 密钥

# 构建
make build

# 运行
make run
```

### 3. 初始化

在浏览器中打开 http://localhost:8080。完成初始化向导以创建第一位管理员用户。

> 出于安全考虑，初始化端点默认仅可从本地主机访问。

### 4. 第一篇文档

导航到文库页面并点击「添加文档」。粘贴 Markdown 内容并选择摄入模式：

- **自动** — 让 TierSum 根据内容长度和配额决定
- **热** — LLM 语义分析章节抽取（完整分析，含摘要、标签与渐进式查询）
- **冷** — Markdown 语法章节抽取（按标题拆分、BM25 + 向量混合检索，均以章节为粒度）

---

## 许可证

MIT 许可证开源
